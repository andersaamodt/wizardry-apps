# Shell Reference Adapter

This adapter exposes reference shell semantics for parity tests against `wizardry-core` fast-path behavior.

Use `rpc-shell-reference` in tests to compare method-level shape and key semantics.
