#!/bin/sh

# Disable the site daemon from starting on boot.

case "${1-}" in
--help|--usage|-h)
  cat <<'USAGE'
Usage: disable-site-daemon SITENAME

Disables the systemd/launchd daemon from starting on boot for a site.

Example: disable-site-daemon mysite
USAGE
  exit 0
  ;;
esac

set -eu
. env-clear

site_name=${1-}
if [ -z "$site_name" ]; then
  die 2 "disable-site-daemon: SITENAME required"
fi
case "$site_name" in
  .|..|*[!a-zA-Z0-9._-]*)
    die 2 "disable-site-daemon: invalid site name"
    ;;
esac

web_root=${WEB_WIZARDRY_ROOT:-$HOME/sites}
sites_dir="$web_root"
site_dir="$sites_dir/$site_name"

if [ ! -d "$site_dir" ]; then
  die "disable-site-daemon: site '$site_name' not found"
fi

disable_run_priv() {
  if [ "$(id -u)" -eq 0 ]; then
    "$@"
    return
  fi
  if has sudo; then
    if tty >/dev/null 2>&1; then
      sudo "$@" </dev/tty
    else
      sudo "$@"
    fi
    return
  fi
  die "disable-site-daemon: root or sudo is required"
}

unit="wizardry-site-${site_name}.service"
plist="${LAUNCHD_PLIST_DIR:-/Library/LaunchDaemons}/org.wizardry.web.${site_name}.plist"

disable_plist_bool_value() {
  key_name=$1
  awk -v key="$key_name" '
    $0 ~ "<key>" key "</key>" { found=1; next }
    found {
      if ($0 ~ /<true\/>/) { print "true"; exit }
      if ($0 ~ /<false\/>/) { print "false"; exit }
      if ($0 ~ /<key>/ || $0 ~ /<\/dict>/) { exit }
    }
  ' "$plist"
}

disable_set_plist_bool() {
  key_name=$1
  bool_value=$2
  tmp_file=$(temp-file disable-site-daemon)

  awk -v key="$key_name" -v val="$bool_value" '
    $0 ~ "<key>" key "</key>" {
      print
      if (getline nextline > 0) {
        if (nextline ~ /<true\/>|<false\/>/) {
          gsub(/<true\/>|<false\/>/, "<" val "/>", nextline)
          print nextline
          replaced=1
          next
        }
        print "  <" val "/>"
        print nextline
        replaced=1
        next
      }
      print "  <" val "/>"
      replaced=1
      next
    }
    $0 ~ "</dict>" && !replaced && !inserted {
      print "  <key>" key "</key>"
      print "  <" val "/>"
      inserted=1
    }
    { print }
    END {
      if (!replaced && !inserted) {
        print "  <key>" key "</key>"
        print "  <" val "/>"
      }
    }
  ' "$plist" > "$tmp_file"

  disable_run_priv cp "$tmp_file" "$plist"
  rm -f "$tmp_file"
}

if [ "$(uname -s 2>/dev/null)" = "Darwin" ] && has launchctl; then
  if [ ! -f "$plist" ]; then
    die "disable-site-daemon: daemon not configured - run 'repair-site-daemon $site_name' first"
  fi

  label="org.wizardry.web.${site_name}"

  # Startup behavior on macOS is controlled by plist flags, not launchctl disable.
  # This keeps startup toggle independent from manual start/stop.
  disable_set_plist_bool RunAtLoad false
  disable_set_plist_bool KeepAlive false

  # Compatibility: clear stale disabled overrides from older implementations.
  disable_run_priv launchctl enable "system/$label" 2>/dev/null || true

  run_at_load=$(disable_plist_bool_value RunAtLoad || printf '')
  keep_alive=$(disable_plist_bool_value KeepAlive || printf '')
  if [ "$run_at_load" != "false" ] || [ "$keep_alive" != "false" ]; then
    die "disable-site-daemon: failed to disable daemon on startup for $site_name"
  fi
  success "Daemon disabled on startup for $site_name"
  exit 0
fi

if has systemctl; then
  disable_run_priv systemctl disable "$unit" 2>/dev/null || true
  success "Daemon disabled on startup for $site_name"
  exit 0
fi

die "disable-site-daemon: systemd or launchd is required"
