#!/bin/sh

case "${1-}" in
--help|--usage|-h)
  cat <<'USAGE'
Usage: install-wizardry-forge [ROOT_DIR]

Installs user-local launchers for Wizardry Forge.
Default root is the current repository.
USAGE
  exit 0
  ;;
esac

require-wizardry || exit 1

set -eu
. env-clear

script_dir=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
repo_root=$(CDPATH= cd -- "$script_dir/../../.." && pwd)
root=${1:-$repo_root}

exec sh "$repo_root/tools/forge/install-forge.sh" --root "$root"
