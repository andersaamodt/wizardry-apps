#!/bin/sh

case "${1-}" in
--help|--usage|-h)
  cat <<'USAGE'
Usage: wizardry-projects-status

Reports installation status for wizardry-apps and Theurgy.
USAGE
  exit 0
  ;;
esac

require-wizardry || exit 1

set -eu
. env-clear

script_dir=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
arcana_dir=$(CDPATH= cd -- "$script_dir/.." && pwd)

installed=0
total=2

if "$arcana_dir/wizardry-apps/wizardry-apps-status" >/dev/null 2>&1; then
  wizardry_apps_status=$("$arcana_dir/wizardry-apps/wizardry-apps-status")
  case "$wizardry_apps_status" in
    installed|partial*) installed=$((installed + 1)) ;;
  esac
fi

if "$arcana_dir/theurgy/is-theurgy-installed" >/dev/null 2>&1; then
  installed=$((installed + 1))
fi

if [ "$installed" -eq 0 ]; then
  printf 'not installed\n'
elif [ "$installed" -eq "$total" ]; then
  printf 'installed\n'
else
  printf 'partial install\n'
fi

