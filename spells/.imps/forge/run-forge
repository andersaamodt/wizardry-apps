#!/bin/sh
# Launch App Forge from this wizardry-apps checkout.

set -eu

ROOT_DIR=$(CDPATH= cd -- "$(dirname "$0")/../../.." && pwd -P)
exec "$ROOT_DIR/tools/forge/launch-forge" --root "$ROOT_DIR" "$@"
