const { assert } = require('chai');
const flakyModule = require('../src/flakyModule'); // Adjust the path as necessary
describe('Flaky Module Tests', function() {
  let seed;
  before(() => {
   Error: an error was encountered while running the model: unexpected EOF